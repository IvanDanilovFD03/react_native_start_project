import { StyleSheet } from "react-native";
import { GlobalColors } from "utils/styles";

export const styles = StyleSheet.create({
  root: {
    padding: 32,
    backgroundColor: GlobalColors.colors.SystemIndigo,
  },
});
