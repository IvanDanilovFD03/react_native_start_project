import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {},
  buttonContainer: {
    borderRadius: 24,
    padding: 6,
    marginHorizontal: 8,
    marginVertical: 2,
  },
  pressed: {
    opacity: 0.75,
  },
});
